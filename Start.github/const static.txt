1.const就是只读的意思,只在声明中使用;
2.static一般有2个作用,规定作用域和存储方式.对于局部变量,static规定其为静态存储方式,每次调用的初始值为上一次调用的值,调用结束后存储空间不释放。

3.静态变量和动态变量：
静态变量在函数调用完后不会被释放，动态变量在调用完后被释放。动态变量只能出现在函数内部，全局变量是静态变量，静态变量只是初始化一次，再次初始化时无效。

4.只读变量：
只读变量不是常量，只读变量不是静态变量。
