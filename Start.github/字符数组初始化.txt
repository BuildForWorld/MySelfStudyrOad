char *a = NULL； 等价于 char *a = {0};
因为{0} 就是0，0代表00000000，所以地址就是00000000。
char *a={'a','b'}会报错，因为字符指针初始化只能是地址{'a','b'}不能返回地址，但{‘a’}可以成功初始化，因为{‘a’}就是’a‘，对应ASCII为97，char *a=97 也就是说 a的地址为00000097，16进制的97是64所以十六进制地址是00000064。

